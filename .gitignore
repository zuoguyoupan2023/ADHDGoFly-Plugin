# Cloudflare Pages - 只发布landing page相关文件
# 插件源代码不参与pages部署

# 开发和测试文件
test*.html
*.md
.vscode/

# 插件压缩包（保留用于直接下载）
# ADHDGoFly-Plugin.7z

# 临时文件
.DS_Store
*.log
*.tmp

# 版本控制
.git/
.gitattributes

# 说明：
# index.html - landing page（会被部署）
# ADHDGoFly-Plugin.7z - 插件压缩包（会被部署用于直接下载）
# 其他所有插件源代码文件不会被部署到Cloudflare Pages